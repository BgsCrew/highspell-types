{
  "name": "@bgscrew/highspell-build-plugin",
  "version": "0.1.0",
  "description": "A Babel plugin to transpile HighSpell plugin code.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"No tests implemented yet\" && exit 0",
    "format": "prettier --write .",
    "check": "pnpm format --check"
  },
  "dependencies": {
    "@bgscrew/highspell-types": "workspace:*"
  },
  "devDependencies": {
    "@babel/core": "^7.28.0"
  }
}
